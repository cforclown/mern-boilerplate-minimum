import styled from 'styled-components';
import { DashboardBase } from './Dashboard';

const Dashboard = styled(DashboardBase)`
  padding: 1rem 1.4rem;
`;
Dashboard.displayName = 'Explore';

export default Dashboard;
