import styled from 'styled-components';

import { AppBase } from './App';

export const App = styled(AppBase)`
  overflow: hidden;
  margin: 0px;
  padding: 0px;
`;

App.displayName = 'App';

export default App;
